# TegasFX Proxy Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration - comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,https://your-app.vercel.app

# Encryption Configuration
# This key must be exactly 32 characters and match your client-side PROXY_ENCRYPTION_KEY
ENCRYPTION_KEY="your-32-character-encryption-key-here"

# Encrypted API Keys
# Use the encrypt-keys.js script to generate these encrypted values
# Example format (replace with your actual encrypted values):
# ENCRYPTED_API_KEY='{"encrypted":"abc123...","iv":"def456...","authTag":"..."}'

# Optional: Multiple encrypted API keys for different environments
# ENCRYPTED_API_KEY_PROD='{"encrypted":"...","iv":"...","authTag":"..."}'
# ENCRYPTED_API_KEY_DEV='{"encrypted":"...","iv":"...","authTag":"..."}'

# Optional: Additional security settings
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=1000